<?xml version="1.0"?>
<launch>
    
    <!-- ARGUMENTS - T265 -->
    <arg name="enable_fisheye"   default="true"/>
    <arg name="linear_accel_cov" default="0.01"/>
    <arg name="initial_reset"    default="true"/>
    
    <!-- ARGUMENTS - RPLIDAR -->
    <arg name="lidarSerial"      default="/dev/ttyUSB_RPLIDAR"/>
    <arg name="lidarName"        default="rplidar"/>
    
    <!-- ARGUMENTS - HECTOR SLAM -->
    <arg name="mappingType" default="2d"/> <!-- 2d or 3d -->
    <arg name="geotiff_map_file_path" default="$(find akros)/maps"/>
    
    <!-- INCLUDE AKROS LAUNCH FILE -->
    <include file="$(find akros)/launch/demo_bringup_$(arg mappingType).launch">
        <arg name="lidarSerial"      value="$(arg lidarSerial)"/>
        <arg name="enable_fisheye"   value="$(arg enable_fisheye)"/>
        <arg name="linear_accel_cov" value="$(arg linear_accel_cov)"/>
        <arg name="initial_reset"    value="$(arg initial_reset)"/>
    </include>

    <!-- INCLUDE HECTOR_MAPPING LAUNCH FILE -->
    <param name="/use_sim_time" value="false"/>
    <include file="$(find akros)/launch/base_launch/hector_mapping.launch">
        <arg name="scan_topic" value="$(arg lidarName)/scan"/>
        <arg name="odom_frame" value="t265_odom_frame"/> <!-- odom_frame -->
        <arg name="base_frame" value="t265_pose_frame"/> <!-- base_link -->
    </include>
    
    <!-- INCLUDE HECTOR_GEOTIFF LAUNCH FILE -->
    <include file="$(find akros)/launch/base_launch/hector_geotiff_mapper.launch">
        <arg name="trajectory_source_frame_name" value="base_link"/>
        <arg name="map_file_path" value="$(arg geotiff_map_file_path)"/>
    </include>

</launch>
